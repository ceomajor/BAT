# 🚀 Деплой на Render.com

## Пошаговая инструкция

### 1. Подготовка репозитория

Загрузите проект на GitHub:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/black-hole-ai.git
git push -u origin main
```

### 2. Создание Web Service на Render

1. Зайдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий `black-hole-ai`

### 3. Настройка сервиса

Заполните поля:

- **Name**: `black-hole-ai` (или любое другое имя)
- **Environment**: `Node`
- **Region**: `Frankfurt (EU Central)` (или ближайший к вам)
- **Branch**: `main`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Instance Type**: `Free` (бесплатный тир)

### 4. Добавление переменных окружения

В разделе **Environment Variables** добавьте:

- **Key**: `GIGACHAT_AUTH_KEY`
- **Value**: `MDE5YTRlY2ItYWZmMS03MTk3LWFiNTctMzE5ZGYxOWQ3NGFiOjJiOTM1ODQ1LTFhYzMtNDBlNy04YTAwLTRmYjkzNDU5YjVlOQ==`

⚠️ **Важно**: Замените значение на ваш реальный API ключ от GigaChat!

### 5. Деплой

1. Нажмите **"Create Web Service"**
2. Render автоматически:
   - Установит зависимости (`npm install`)
   - Запустит сервер (`npm start`)
   - Выдаст вам URL типа `https://black-hole-ai.onrender.com`

### 6. Готово! 🎉

Ваше приложение будет доступно по адресу, который предоставит Render.

## Особенности бесплатного тира

- ✅ Бесплатный SSL сертификат
- ✅ Автоматический деплой при push в GitHub
- ⚠️ Сервис "засыпает" после 15 минут неактивности
- ⚠️ Первый запрос после "сна" может занять 30-60 секунд

## Альтернативные варианты

### Railway.app
- Более щедрый бесплатный тир
- Быстрее "просыпается"
- Проще настройка

### Vercel (требует адаптации)
- Serverless функции
- Мгновенный отклик
- Нужно переписать код под их API

## Обновление приложения

После изменений в коде:
```bash
git add .
git commit -m "Update"
git push
```

Render автоматически задеплоит новую версию.

## Проблемы?

- **Ошибка 503**: Сервис "спит", подождите 30-60 секунд
- **Ошибка авторизации**: Проверьте переменную окружения `GIGACHAT_AUTH_KEY`
- **Не работает API**: Убедитесь, что ваш GigaChat API ключ активен
