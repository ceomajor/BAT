# 🚀 Деплой BAT AI на Vercel

## Подготовка проекта

Проект уже настроен для деплоя на Vercel! Все необходимые файлы созданы:

- ✅ `vercel.json` - конфигурация Vercel
- ✅ `api/auth.js` - serverless функция для авторизации
- ✅ `api/chat.js` - serverless функция для чата
- ✅ Статические файлы (HTML, CSS, JS, изображения)

## Шаги для деплоя

### 1. Установите Vercel CLI (опционально)

```bash
npm install -g vercel
```

### 2. Деплой через веб-интерфейс (рекомендуется)

#### Шаг 1: Создайте аккаунт на Vercel
1. Перейдите на https://vercel.com
2. Зарегистрируйтесь через GitHub, GitLab или email

#### Шаг 2: Загрузите проект на GitHub
1. Создайте новый репозиторий на GitHub
2. Загрузите туда все файлы проекта:

```bash
cd "/Users/sulim/Downloads/Black Hole AI"
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/bat-ai.git
git push -u origin main
```

#### Шаг 3: Импортируйте проект в Vercel
1. Войдите в Vercel Dashboard
2. Нажмите "Add New..." → "Project"
3. Выберите ваш GitHub репозиторий
4. Нажмите "Import"

#### Шаг 4: Настройте переменные окружения
В настройках проекта добавьте:

**Environment Variables:**
- Имя: `GIGACHAT_AUTH_KEY`
- Значение: Ваш ключ авторизации GigaChat (в формате Base64)

Текущий ключ (можно использовать для теста):
```
MDE5YTRlY2ItYWZmMS03MTk3LWFiNTctMzE5ZGYxOWQ3NGFiOjJiOTM1ODQ1LTFhYzMtNDBlNy04YTAwLTRmYjkzNDU5YjVlOQ==
```

#### Шаг 5: Деплой!
1. Нажмите "Deploy"
2. Дождитесь завершения сборки (1-2 минуты)
3. Получите ссылку на ваше приложение!

### 3. Деплой через CLI (альтернатива)

```bash
cd "/Users/sulim/Downloads/Black Hole AI"
vercel
```

Следуйте инструкциям в терминале.

## Настройка переменных окружения через CLI

```bash
vercel env add GIGACHAT_AUTH_KEY
```

Вставьте ваш ключ авторизации GigaChat.

## Структура проекта для Vercel

```
Black Hole AI/
├── api/                    # Serverless функции
│   ├── auth.js            # Авторизация GigaChat
│   └── chat.js            # Чат с GigaChat
├── index.html             # Главная страница
├── script.js              # Клиентский JavaScript
├── styles.css             # Стили
├── *.jpg                  # Изображения
├── vercel.json            # Конфигурация Vercel
├── package.json           # Зависимости
└── README.md              # Документация
```

## Как работает на Vercel

1. **Статические файлы** (HTML, CSS, JS, изображения) раздаются через CDN Vercel
2. **API endpoints** (`/api/auth` и `/api/chat`) работают как serverless функции
3. **Автоматический HTTPS** - Vercel предоставляет SSL сертификат
4. **Глобальный CDN** - быстрая загрузка из любой точки мира

## Проверка работы

После деплоя:

1. Откройте ваш URL (например, `https://bat-ai.vercel.app`)
2. Приложение должно автоматически запроситься и работать
3. Проверьте, что чат отвечает на сообщения

## Обновление проекта

### Через GitHub (автоматически)
Просто сделайте `git push` - Vercel автоматически задеплоит изменения:

```bash
git add .
git commit -m "Update"
git push
```

### Через CLI
```bash
vercel --prod
```

## Получение ключа GigaChat

Если нужен новый ключ:

1. Перейдите на https://developers.sber.ru/studio/workspaces
2. Создайте новый проект
3. Получите Client ID и Client Secret
4. Закодируйте в Base64: `echo -n "client_id:client_secret" | base64`
5. Добавьте в переменные окружения Vercel

## Устранение проблем

### Ошибка 500 на /api/auth
- Проверьте, что `GIGACHAT_AUTH_KEY` добавлен в переменные окружения
- Проверьте формат ключа (должен быть Base64)

### Ошибка CORS
- Уже настроено в `api/auth.js` и `api/chat.js`
- Если проблема сохраняется, проверьте логи в Vercel Dashboard

### Функции не работают
- Проверьте логи в Vercel Dashboard → Functions
- Убедитесь, что `vercel.json` корректный

## Полезные команды

```bash
# Локальная разработка с Vercel
vercel dev

# Просмотр логов
vercel logs

# Список деплоев
vercel ls

# Удалить проект
vercel remove
```

## Преимущества Vercel

✅ **Бесплатный план** - достаточно для личного использования  
✅ **Автоматический HTTPS** - безопасное соединение  
✅ **Глобальный CDN** - быстрая загрузка  
✅ **Serverless функции** - масштабируемость  
✅ **Автодеплой** - при каждом push в GitHub  
✅ **Аналитика** - встроенная статистика  

## Лимиты бесплатного плана

- 100 GB bandwidth/месяц
- 100 GB-hours serverless execution/месяц
- Unlimited deployments
- Unlimited team members

Этого более чем достаточно для личного использования!

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Откройте Issues в GitHub репозитории
3. Документация Vercel: https://vercel.com/docs

---

**Готово!** 🎉 Ваш BAT AI теперь доступен всему миру!
