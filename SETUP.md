# 🚀 Инструкция по запуску Black Hole AI

## Проблема CORS решена!

Теперь приложение использует локальный прокси-сервер для обхода CORS-ограничений.

## 📋 Быстрый старт

### 1. Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит необходимые пакеты:
- `express` - веб-сервер
- `cors` - для обработки CORS
- `nodemon` - автоперезагрузка сервера (опционально)

### 2. Запуск сервера

```bash
npm start
```

Вы увидите сообщение:
```
╔════════════════════════════════════════════╗
║   🚀 GigaChat Proxy Server запущен!       ║
╚════════════════════════════════════════════╝

📍 Адрес: http://localhost:3000
🌐 Откройте: http://localhost:3000/index.html
```

### 3. Открытие приложения

Откройте браузер и перейдите по адресу:
```
http://localhost:3000/index.html
```

**ВАЖНО:** Не открывайте файл напрямую (file://), используйте только http://localhost:3000

### 4. Использование

Теперь можно отправлять сообщения AI! Все запросы будут проходить через локальный прокси-сервер.

## 🛠️ Как это работает

```
Браузер (Frontend)
    ↓
    ↓ HTTP запрос (без CORS)
    ↓
Локальный сервер (localhost:3000)
    ↓
    ↓ HTTPS запрос с правильными заголовками
    ↓
GigaChat API (Сбербанк)
```

### Преимущества:

✅ **Нет CORS ошибок** - браузер делает запросы на тот же домен  
✅ **Безопасность** - API ключи хранятся на сервере  
✅ **Кэширование токенов** - токены переиспользуются  
✅ **Обработка SSL** - сервер корректно работает с сертификатами Сбера  

## 📁 Структура проекта

```
Black Hole AI/
├── index.html          # Frontend интерфейс
├── styles.css          # Стили
├── script.js           # Frontend логика (обновлен для прокси)
├── server.js           # Backend прокси-сервер (НОВЫЙ)
├── package.json        # Зависимости Node.js (НОВЫЙ)
├── README.md           # Основная документация
└── SETUP.md            # Эта инструкция
```

## 🔧 Команды

### Запуск в production режиме
```bash
npm start
```

### Запуск в development режиме (с автоперезагрузкой)
```bash
npm run dev
```

### Остановка сервера
Нажмите `Ctrl + C` в терминале

## ❗ Решение проблем

### Ошибка: "Cannot find module 'express'"
```bash
npm install
```

### Ошибка: "Port 3000 is already in use"
Порт занят другим приложением. Измените порт в `server.js`:
```javascript
const PORT = 3001; // Измените на свободный порт
```

### Ошибка: "Failed to fetch"
Убедитесь, что:
1. Сервер запущен (`npm start`)
2. Вы открываете `http://localhost:3000/index.html`, а не файл напрямую
3. В консоли сервера нет ошибок

### Ошибка авторизации GigaChat
Проверьте:
1. Правильность AUTH_KEY в `server.js`
2. Активность API ключа в личном кабинете Сбера
3. Интернет-соединение

## 🔐 Безопасность

⚠️ **Важно для production:**

1. **Не коммитьте** `server.js` с реальными ключами в публичные репозитории
2. Используйте **переменные окружения** для хранения ключей:
   ```javascript
   const AUTH_KEY = process.env.GIGACHAT_AUTH_KEY;
   ```
3. Добавьте **аутентификацию** для доступа к вашему серверу
4. Используйте **HTTPS** в production
5. Добавьте **rate limiting** для защиты от злоупотреблений

### Пример с переменными окружения:

Создайте файл `.env`:
```
GIGACHAT_AUTH_KEY=MDE5YTRlY2ItYWZmMS03MTk3LWFiNTctMzE5ZGYxOWQ3NGFiOmJkNWUwNDY0LTVkODctNDJhYi05ZGM2LTViYzBkOWY1NWUwZg==
PORT=3000
```

Установите `dotenv`:
```bash
npm install dotenv
```

В `server.js`:
```javascript
require('dotenv').config();
const AUTH_KEY = process.env.GIGACHAT_AUTH_KEY;
const PORT = process.env.PORT || 3000;
```

## 📝 Дополнительная информация

- **Документация GigaChat:** https://developers.sber.ru/docs/ru/gigachat/api/overview
- **Express.js:** https://expressjs.com/
- **CORS:** https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS

## 🎉 Готово!

Теперь ваше приложение работает без CORS ошибок через локальный прокси-сервер.

Приятного общения с AI! 🤖
